<!DOCTYPE html>
<html>

<head>
    <title>Comments</title>
    <%- include('styles'); -%>
    <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
    <div class="section"></div>
    <main>
        <center>
            <div class="container feed-block-container">
                <div class="z-depth-2 feed-block">
                    <div class='row'>
                        <div class="switch col s12">
                        <form method="post">
                        <% if (emailSetting == 1) { %>
                          <label>
                              Do not send emails
                              <input type="checkbox" name="email_enabled" checked="checked">
                              <span class="lever"></span>
                              Send emails
                          </label>
                        <% } else { %>
                            <label>
                                Do not send emails
                                <input type="checkbox" name="email_enabled">
                                <span class="lever"></span>
                                Send emails
                            </label>
                        <% } %>
                         
                        </div>
                      </div>
                    <div class="row">
                            <button type='submit' name='btn_search'
                            class='col s2 offset-s6 btn btn-large waves-effect indigo'>Submit</button>
                            </form>  
                        <a class='col s2 offset-s2 btn btn-large waves-effect indigo' style="position: relative; right: 10px;" href="/profile/<%=id%>">Return</a>
                    </div>
                    </div>
                </div>
            </div>
            </div>
        </center>
    </main>
</body>