<!DOCTYPE html>
<html>

<head>
    <title>Tags</title>
    <%- include('styles'); -%>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <!-- Include stylesheet -->
    <link href="https://cdn.quilljs.com/1.3.6/quill.snow.css" rel="stylesheet">
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.7.1/jquery.min.js" type="text/javascript"></script>
</head>

<body>
    <div class="section"></div>
    <main>
        <center>
            <div class="container" style="margin-bottom: -15px;">
                <div class="row">
                    <h1>Edit tags</h1>
                </div>
        </center>
        <div class="container" style="min-width: 95%;  padding: 3vh 0vh 0vh 0vh;">
            <form class="z-depth-2 row"
                style="display: inline-block; min-width: 100%; padding: 2vh 2vh 1vh; border: 1px solid #EEE; background-color: #f3e5f5"
                method="post">
                <div class="container z-depth-2 row" style="min-width: 90%; background-color: #EEE; padding: 2vh 2vh;">
                    <%if (tags.length > 0) { %>
                    <ul>
                        <% for(var i = 0; i < tags.length; i++) {%>
                        <li><%= tags[i].tag %></li>
                        <% } %>
                    </ul>
                    <% } %>
                </div>
                <div class='row' style="margin-bottom: 5px;">
                    <div class='input-field col s12'>
                        <input class='validate' type='text' name='tags' id='tags' />
                        <label for='tags'>Type some tags (space separated)</label>
                    </div>
                </div>
                <div class='row'>
                    <button type='submit' name='btn_submit' class='col s2 offset-s7 btn btn-large waves-effect indigo'
                        value='add'>Add</button>
                    <button type='submit' name='btn_submit' class='col s2 offset-s1 btn btn-large waves-effect indigo'
                        value='del'>Delete</button>
                    <%if (errors) { %>
                    <ul>
                        <% for(var i = 0; i < errors.length; i++) {%>
                        <li><%= errors[i].msg %></li>
                        <% } %>
                    </ul>
                    <% } %>
                </div>
            </form>
        </div>
    </main>
</body>